# This nginx.conf is no longer used.
# Each app now has its own nginx config:
# - apps/web/nginx.conf  (serves Web POS on port 80)
# - apps/admin/nginx.conf (serves Admin Panel on port 80)
#
# The main nginx.conf has been replaced by per-app nginx configurations
# to simplify routing and avoid path-based conflicts.
